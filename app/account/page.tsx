import React from "react";

type Props = {};

const page = (props: Props) => {
  return (
    <div className="h-full w-full flex justify-center items-center">
      <div className="text-xl font-black uppercase">Default page</div>
    </div>
  );
};

export default page;
